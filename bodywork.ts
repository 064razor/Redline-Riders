export const Bodywork = {
    cars: {
        maruMk5: {
            id: "maruMk5",
            displayName: "Maru MK-5",
            inspiration: "Lightweight roadster inspired by the MX-5",

            defaultPaint: "#ffffff",
            windowColor: "#111a2e",

            sprite: {
                imagePath: "./assets/cars/maru-mk5/body-white.png",
                xOffset: -12,
                yOffset: -14,
                width: 96,
                height: 75,

                rearWheel: {
                    x: 11,
                    y: 33,
                    radius: 7
                },

                frontWheel: {
                    x: 68,
                    y: 33,
                    radius: 7
                }
            },

            rims: {
                classic5: "Classic 5-Spoke",
                split6: "Classic Split 6-Spoke",
                mesh: "Classic Street Mesh",
                deepDish: "Classic Deep Dish",
                star: "Classic Star Racer",
                multiSpoke: "Multi-Spoke",
                turbine: "Turbine",
                fiveStar: "Five-Star",
                dragDish: "Drag Dish",
                splitMesh: "Split Mesh",
                hyper5: "Hyper 5-Spoke",
                gtMesh: "GT Mesh",
                trackTwin: "Track Twin-Spoke",
                retroSlot: "Retro Slot Mag",
                muscleChrome: "Muscle Chrome",
                heritageWire: "Heritage Wire"
            }
        },

        swagGG2: {
            id: "swagGG2",
            displayName: "Swag GG 2",
            inspiration: "Lightweight 70s-style hot hatch",

            defaultPaint: "#ffffff",
            windowColor: "#111a2e",

            sprite: {
                imagePath: "./assets/cars/swag-gg-2/body-white.png",
                xOffset: -9,
                yOffset: -10,
                width: 104,
                height: 50,

                rearWheel: {
                    x: 16,
                    y: 30,
                    radius: 3
                },

                frontWheel: {
                    x: 72,
                    y: 30,
                    radius: 3
                }
            },

            rims: {
                classic5: "Classic 5-Spoke",
                split6: "Classic Split 6-Spoke",
                mesh: "Classic Street Mesh",
                deepDish: "Classic Deep Dish",
                star: "Classic Star Racer",
                multiSpoke: "Multi-Spoke",
                turbine: "Turbine",
                fiveStar: "Five-Star",
                dragDish: "Drag Dish",
                splitMesh: "Split Mesh",
                hyper5: "Hyper 5-Spoke",
                gtMesh: "GT Mesh",
                trackTwin: "Track Twin-Spoke",
                retroSlot: "Retro Slot Mag",
                muscleChrome: "Muscle Chrome",
                heritageWire: "Heritage Wire"
            }
        },

        rouletteBlair: {
            id: "rouletteBlair",
            displayName: "Roulette Blair",
            inspiration: "Classic Bel-Air-inspired heavyweight muscle cruiser",

            defaultPaint: "#ffffff",
            windowColor: "#111a2e",

            sprite: {
                imagePath: "./assets/cars/roulette-blair/body-white.png",
                xOffset: -23,
                yOffset: -19,
                width: 115,
                height: 80,

                rearWheel: {
                    x: 7,
                    y: 32,
                    radius: 8
                },

                frontWheel: {
                    x: 70,
                    y: 32,
                    radius: 8
                }
            },

            rims: {
                classic5: "Classic 5-Spoke",
                split6: "Classic Split 6-Spoke",
                mesh: "Classic Street Mesh",
                deepDish: "Classic Deep Dish",
                star: "Classic Star Racer",
                multiSpoke: "Multi-Spoke",
                turbine: "Turbine",
                fiveStar: "Five-Star",
                dragDish: "Drag Dish",
                splitMesh: "Split Mesh",
                hyper5: "Hyper 5-Spoke",
                gtMesh: "GT Mesh",
                trackTwin: "Track Twin-Spoke",
                retroSlot: "Retro Slot Mag",
                muscleChrome: "Muscle Chrome",
                heritageWire: "Heritage Wire"
            }
        },

        rouletteMontBlanc: {
            id: "rouletteMontBlanc",
            displayName: "Roulette Mont Blanc",
            inspiration: "1981 Monte Carlo-inspired mid-muscle coupe",

            defaultPaint: "#ffffff",
            windowColor: "#111a2e",

            sprite: {
                imagePath: "./assets/cars/roulette-mont-blanc/body-white.png",
                xOffset: -17,
                yOffset: -22,
                width: 110,
                height: 82,

                rearWheel: {
                    x: 11,
                    y: 30,
                    radius: 8
                },

                frontWheel: {
                    x: 70,
                    y: 30,
                    radius: 8
                }
            },

            rims: {
                classic5: "Classic 5-Spoke",
                split6: "Classic Split 6-Spoke",
                mesh: "Classic Street Mesh",
                deepDish: "Classic Deep Dish",
                star: "Classic Star Racer",
                multiSpoke: "Multi-Spoke",
                turbine: "Turbine",
                fiveStar: "Five-Star",
                dragDish: "Drag Dish",
                splitMesh: "Split Mesh",
                hyper5: "Hyper 5-Spoke",
                gtMesh: "GT Mesh",
                trackTwin: "Track Twin-Spoke",
                retroSlot: "Retro Slot Mag",
                muscleChrome: "Muscle Chrome",
                heritageWire: "Heritage Wire"
            }
        },
	 
        hannaCivilian: {
				id: "hannaCivilian",
				displayName: "Hanna Civilian",
				inspiration: "1999 compact coupe-inspired lightweight tuner",

			defaultPaint: "#ffffff",
			windowColor: "#111a2e",

			sprite: {
				imagePath: "./assets/cars/hanna-civilian/body-white.png",
				xOffset: -22,
				yOffset: -16,
				width: 109,
				height: 71,

				rearWheel: {
					x: 1,
					y: 31,
					radius: 4
				},

				frontWheel: {
					x: 63,
					y: 31,
					radius: 4
				}
			},

			rims: {
				classic5: "Classic 5-Spoke",
				split6: "Classic Split 6-Spoke",
				mesh: "Classic Street Mesh",
				deepDish: "Classic Deep Dish",
				star: "Classic Star Racer",
				multiSpoke: "Multi-Spoke",
				turbine: "Turbine",
				fiveStar: "Five-Star",
				dragDish: "Drag Dish",
				splitMesh: "Split Mesh",
				hyper5: "Hyper 5-Spoke",
				gtMesh: "GT Mesh",
				trackTwin: "Track Twin-Spoke",
				retroSlot: "Retro Slot Mag",
				muscleChrome: "Muscle Chrome",
				heritageWire: "Heritage Wire"
			}
        },

    },
    drawRim(
        ctx: CanvasRenderingContext2D,
        style: string,
        x: number,
        y: number,
        rotation: number = 0,
        blur: number = 0
    ) {
        const radius = 8;
        const innerRadius = 3;
        const blurAlpha = Math.max(0.25, 1 - blur * 0.08);

        const tireColor = "#050505";
        const barrelColor = "#1a1a1a";
        const rimColor = "#dcdcdc";
        const rimShadow = "#777777";
        const hubColor = "#d0d0d0";

        ctx.save();
        ctx.translate(x, y);
        ctx.rotate(rotation);
        ctx.globalAlpha = blurAlpha;
        ctx.lineCap = "round";
        ctx.lineJoin = "round";

        // ===== TIRE =====
        ctx.fillStyle = tireColor;
        ctx.beginPath();
        ctx.arc(0, 0, radius + 3, 0, Math.PI * 2);
        ctx.fill();

        // ===== DARK INNER BARREL =====
        // This is the important color fix: the wheel face is dark,
        // and the spokes/details are light alloy. No more inverted white discs.
        ctx.fillStyle = barrelColor;
        ctx.beginPath();
        ctx.arc(0, 0, radius + 1, 0, Math.PI * 2);
        ctx.fill();

        // ===== OUTER ALLOY LIP =====
        ctx.strokeStyle = rimColor;
        ctx.lineWidth = 2;
        ctx.beginPath();
        ctx.arc(0, 0, radius, 0, Math.PI * 2);
        ctx.stroke();

        ctx.strokeStyle = rimShadow;
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.arc(0, 0, radius - 2, 0, Math.PI * 2);
        ctx.stroke();

        ctx.strokeStyle = rimColor;
        ctx.fillStyle = rimColor;
        ctx.lineWidth = 2;

        switch (style) {
            case "split6":
                for (let i = 0; i < 6; i++) {
                    const angle = (Math.PI * 2 / 6) * i;
                    const spread = 0.12;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle - spread) * innerRadius, Math.sin(angle - spread) * innerRadius);
                    ctx.lineTo(Math.cos(angle - spread) * (radius - 2), Math.sin(angle - spread) * (radius - 2));
                    ctx.stroke();

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle + spread) * innerRadius, Math.sin(angle + spread) * innerRadius);
                    ctx.lineTo(Math.cos(angle + spread) * (radius - 2), Math.sin(angle + spread) * (radius - 2));
                    ctx.stroke();
                }
                break;

            case "mesh":
                ctx.lineWidth = 1;

                for (let i = 0; i < 8; i++) {
                    const angle = (Math.PI * 2 / 8) * i;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * 2, Math.sin(angle) * 2);
                    ctx.lineTo(Math.cos(angle + 0.45) * (radius - 2), Math.sin(angle + 0.45) * (radius - 2));
                    ctx.stroke();

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * 2, Math.sin(angle) * 2);
                    ctx.lineTo(Math.cos(angle - 0.45) * (radius - 2), Math.sin(angle - 0.45) * (radius - 2));
                    ctx.stroke();
                }
                break;

            case "deepDish":
                ctx.strokeStyle = rimColor;
                ctx.lineWidth = 2;
                ctx.beginPath();
                ctx.arc(0, 0, radius - 2, 0, Math.PI * 2);
                ctx.stroke();

                for (let i = 0; i < 5; i++) {
                    const angle = (Math.PI * 2 / 5) * i;
                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * innerRadius, Math.sin(angle) * innerRadius);
                    ctx.lineTo(Math.cos(angle) * (radius - 4), Math.sin(angle) * (radius - 4));
                    ctx.stroke();
                }
                break;

            case "star":
                ctx.fillStyle = rimColor;
                ctx.beginPath();

                for (let i = 0; i < 10; i++) {
                    const angle = (Math.PI * 2 / 10) * i - Math.PI / 2;
                    const pointRadius = i % 2 === 0 ? radius - 1 : innerRadius;
                    const px = Math.cos(angle) * pointRadius;
                    const py = Math.sin(angle) * pointRadius;

                    if (i === 0) {
                        ctx.moveTo(px, py);
                    }
                    else {
                        ctx.lineTo(px, py);
                    }
                }

                ctx.closePath();
                ctx.fill();
                break;

            case "multiSpoke":
                ctx.lineWidth = 1;

                for (let i = 0; i < 12; i++) {
                    const angle = (Math.PI * 2 / 12) * i;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * 2, Math.sin(angle) * 2);
                    ctx.lineTo(Math.cos(angle) * (radius - 2), Math.sin(angle) * (radius - 2));
                    ctx.stroke();
                }
                break;

            case "turbine":
                ctx.lineWidth = 2;

                for (let i = 0; i < 8; i++) {
                    const angle = (Math.PI * 2 / 8) * i;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * innerRadius, Math.sin(angle) * innerRadius);
                    ctx.quadraticCurveTo(
                        Math.cos(angle + 0.25) * 6,
                        Math.sin(angle + 0.25) * 6,
                        Math.cos(angle + 0.55) * (radius - 2),
                        Math.sin(angle + 0.55) * (radius - 2)
                    );
                    ctx.stroke();
                }
                break;

            case "fiveStar":
                ctx.lineWidth = 3;

                for (let i = 0; i < 5; i++) {
                    const angle = (Math.PI * 2 / 5) * i;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * innerRadius, Math.sin(angle) * innerRadius);
                    ctx.lineTo(Math.cos(angle) * (radius - 1), Math.sin(angle) * (radius - 1));
                    ctx.stroke();
                }
                break;

            case "dragDish":
                ctx.strokeStyle = rimColor;
                ctx.fillStyle = barrelColor;
                ctx.lineWidth = 2;

                ctx.beginPath();
                ctx.arc(0, 0, radius - 1, 0, Math.PI * 2);
                ctx.fill();
                ctx.stroke();

                ctx.fillStyle = rimColor;

                for (let i = 0; i < 10; i++) {
                    const angle = (Math.PI * 2 / 10) * i;
                    ctx.beginPath();
                    ctx.arc(
                        Math.cos(angle) * 4.6,
                        Math.sin(angle) * 4.6,
                        0.7,
                        0,
                        Math.PI * 2
                    );
                    ctx.fill();
                }
                break;

            case "splitMesh":
                ctx.lineWidth = 1;

                for (let i = 0; i < 10; i++) {
                    const angle = (Math.PI * 2 / 10) * i;
                    const spread = 0.09;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle - spread) * 2, Math.sin(angle - spread) * 2);
                    ctx.lineTo(Math.cos(angle + 0.32) * (radius - 2), Math.sin(angle + 0.32) * (radius - 2));
                    ctx.stroke();

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle + spread) * 2, Math.sin(angle + spread) * 2);
                    ctx.lineTo(Math.cos(angle - 0.32) * (radius - 2), Math.sin(angle - 0.32) * (radius - 2));
                    ctx.stroke();
                }
                break;

            case "hyper5":
                ctx.lineWidth = 2;

                for (let i = 0; i < 5; i++) {
                    const angle = (Math.PI * 2 / 5) * i;
                    const bladeAngle = angle + 0.18;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * innerRadius, Math.sin(angle) * innerRadius);
                    ctx.lineTo(Math.cos(bladeAngle) * (radius - 1), Math.sin(bladeAngle) * (radius - 1));
                    ctx.stroke();
                }
                break;

            case "gtMesh":
                ctx.lineWidth = 1;

                for (let i = 0; i < 12; i++) {
                    const angle = (Math.PI * 2 / 12) * i;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * 2, Math.sin(angle) * 2);
                    ctx.lineTo(Math.cos(angle + 0.28) * (radius - 2), Math.sin(angle + 0.28) * (radius - 2));
                    ctx.stroke();

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * 2, Math.sin(angle) * 2);
                    ctx.lineTo(Math.cos(angle - 0.28) * (radius - 2), Math.sin(angle - 0.28) * (radius - 2));
                    ctx.stroke();
                }

                ctx.beginPath();
                ctx.arc(0, 0, radius - 4, 0, Math.PI * 2);
                ctx.stroke();
                break;

            case "trackTwin":
                ctx.lineWidth = 2;

                for (let i = 0; i < 5; i++) {
                    const angle = (Math.PI * 2 / 5) * i;
                    const spread = 0.14;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle - spread) * innerRadius, Math.sin(angle - spread) * innerRadius);
                    ctx.lineTo(Math.cos(angle - spread) * (radius - 1), Math.sin(angle - spread) * (radius - 1));
                    ctx.stroke();

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle + spread) * innerRadius, Math.sin(angle + spread) * innerRadius);
                    ctx.lineTo(Math.cos(angle + spread) * (radius - 1), Math.sin(angle + spread) * (radius - 1));
                    ctx.stroke();
                }
                break;

            case "retroSlot":
                ctx.lineWidth = 2;

                ctx.beginPath();
                ctx.arc(0, 0, radius - 2, 0, Math.PI * 2);
                ctx.stroke();

                ctx.fillStyle = rimColor;
                for (let i = 0; i < 5; i++) {
                    const angle = (Math.PI * 2 / 5) * i;

                    ctx.save();
                    ctx.rotate(angle);
                    ctx.beginPath();
                    ctx.ellipse(0, -4.6, 1.1, 2.5, 0, 0, Math.PI * 2);
                    ctx.fill();
                    ctx.restore();
                }
                break;

            case "muscleChrome":
                ctx.lineWidth = 2;
                ctx.beginPath();
                ctx.arc(0, 0, radius - 2, 0, Math.PI * 2);
                ctx.stroke();

                ctx.lineWidth = 3;
                for (let i = 0; i < 5; i++) {
                    const angle = (Math.PI * 2 / 5) * i;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * innerRadius, Math.sin(angle) * innerRadius);
                    ctx.lineTo(Math.cos(angle) * (radius - 3), Math.sin(angle) * (radius - 3));
                    ctx.stroke();
                }
                break;

            case "heritageWire":
                ctx.lineWidth = 1;

                for (let i = 0; i < 16; i++) {
                    const angle = (Math.PI * 2 / 16) * i;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * 2, Math.sin(angle) * 2);
                    ctx.lineTo(Math.cos(angle + 0.42) * (radius - 2), Math.sin(angle + 0.42) * (radius - 2));
                    ctx.stroke();

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * 2, Math.sin(angle) * 2);
                    ctx.lineTo(Math.cos(angle - 0.42) * (radius - 2), Math.sin(angle - 0.42) * (radius - 2));
                    ctx.stroke();
                }
                break;

            case "classic5":
            default:
                for (let i = 0; i < 5; i++) {
                    const angle = (Math.PI * 2 / 5) * i;

                    ctx.beginPath();
                    ctx.moveTo(Math.cos(angle) * innerRadius, Math.sin(angle) * innerRadius);
                    ctx.lineTo(Math.cos(angle) * (radius - 2), Math.sin(angle) * (radius - 2));
                    ctx.stroke();
                }
                break;
        }

        // Hub cap
        ctx.fillStyle = hubColor;
        ctx.beginPath();
        ctx.arc(0, 0, 2.2, 0, Math.PI * 2);
        ctx.fill();

        ctx.strokeStyle = tireColor;
        ctx.lineWidth = 0.8;
        ctx.beginPath();
        ctx.arc(0, 0, 2.2, 0, Math.PI * 2);
        ctx.stroke();

        ctx.restore();
    }
};
